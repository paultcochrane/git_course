\begin{tikzpicture}
[
    general/.style={rectangle,
		fill=green!20!white,
		draw=green!50!black,
		minimum width=30mm,
		minimum height=10mm,
                rounded corners,
            },
    ssh/.style={rectangle,
		fill=red!20!white,
		draw=red!50!black,
		minimum width=30mm,
		minimum height=10mm,
                rounded corners,
            },
    git/.style={rectangle,
		fill=blue!20!white,
		draw=blue!50!black,
		minimum width=30mm,
		minimum height=10mm,
                rounded corners,
            },
    link/.style={->, shorten <=1pt, shorten >=1pt, >=stealth, semithick},
]

% course introduction
\node (intro) [general] { Introduction };
\node (history) [general,below=of intro] { History };
\node (concepts) [general,below=of history] { Concepts };

\draw [link] (intro) -- (history);
\draw [link] (history) -- (concepts);

% github?

%SSH  -> Windows
%        -> Cygwin
%        -> PuTTY
%        -> TortoiseGIT
%        -> TortoiseSVN
%     -> Linux

% SSH
\node (linux) at (100mm,0mm)  [ssh]   { Linux };
\node (linux-ssh)   [ssh,below=of linux]   { ssh };

\draw [link] (linux) -- (linux-ssh);

\node (windows)     [ssh,right=of linux]   { Windows };
\node (putty)       [ssh,below=of windows] { PuTTY };
\node (tortoise)    [ssh,below=of putty]   { Tortoise GIT };

\draw [link] (windows) -- (putty);
\draw [link] (putty)   -- (tortoise);

\begin{pgfonlayer}{background}
    \node [
        fill=gray!30,
        fit=(linux) (windows) (tortoise),
        rounded corners,
    ]{};
\end{pgfonlayer}

% GIT
\node (git-history)     [git,below=of concepts]        { History };
\node (git-concepts)    [git,below=of git-history]     { Concepts };
\node (git-first-steps) [git,below=of git-concepts]    { First steps };
\node (git-config)      [git,below=of git-first-steps] { Configuration };
% add, commit, delete etc.
\node (git-workflow)    [git,below=of git-config]      { Workflow };
\node (git-branches)    [git,below=of git-workflow]    { Branches };
\node (git-merging)     [git,below=of git-branches]    { Merging };

\draw [link] (git-history)     -- (git-concepts);
\draw [link] (git-concepts)    -- (git-first-steps);
\draw [link] (git-first-steps) -- (git-config);
\draw [link] (git-config)      -- (git-workflow);
\draw [link] (git-workflow)    -- (git-branches);
\draw [link] (git-branches)    -- (git-merging);


% SVN
% history
% concepts
% first steps
% config
% workflow
% metadata (properties)
% branches
% merging
% tagging

\end{tikzpicture}

% vim: expandtab shiftwidth=4 softtabstop=4
